<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>My Google AI Studio App</title>

<style>
  body {
    font-family: Arial, sans-serif;
    margin: 0;
    background: #f5f7fa;
    display: flex;
    justify-content: center;
    padding: 40px 20px;
  }
  .container {
    background: white;
    padding: 30px;
    border-radius: 14px;
    max-width: 500px;
    width: 100%;
    box-shadow: 0 4px 18px rgba(0,0,0,0.08);
  }
  h1 {
    text-align: center;
    margin-top: 0;
    color: #333;
    font-size: 24px;
  }
  label {
    font-weight: bold;
    display: block;
    margin-bottom: 8px;
    margin-top: 18px;
  }
  textarea, input[type="file"] {
    width: 100%;
    padding: 12px;
    border-radius: 8px;
    border: 1px solid #ccc;
    font-size: 15px;
  }
  button {
    width: 100%;
    padding: 14px;
    background: #2563eb;
    color: white;
    font-size: 16px;
    border: none;
    border-radius: 10px;
    margin-top: 20px;
    cursor: pointer;
  }
  button:hover {
    background: #1e4fc7;
  }
  .result-box {
    margin-top: 25px;
    padding: 18px;
    border-radius: 10px;
    background: #eef2ff;
    color: #222;
    white-space: pre-wrap;
    font-size: 15px;
  }
</style>
</head>

<body>
<div class="container">
  <h1>My Google AI Studio App</h1>

  <label for="inputText">Enter text</label>
  <textarea id="inputText" rows="4" placeholder="Type something for the model..."></textarea>

  <label for="inputImage">Or upload an image</label>
  <input type="file" id="inputImage" accept="image/*" />

  <button id="submitBtn">Submit</button>

  <div id="result" class="result-box" style="display:none;"></div>
</div>

<script>
document.getElementById("submitBtn").onclick = async function () {
  const text = document.getElementById("inputText").value.trim();
  const imageFile = document.getElementById("inputImage").files[0];
  const resultBox = document.getElementById("result");

  const formData = new FormData();
  if (text) formData.append("text", text);
  if (imageFile) formData.append("image", imageFile);

  resultBox.style.display = "block";
  resultBox.textContent = "Loading...";

  try {
    const response = await fetch("API_URL/predict", {
      method: "POST",
      body: formData
    });

    if (!response.ok) {
      resultBox.textContent = "Error: " + response.statusText;
      return;
    }

    const data = await response.json();
    resultBox.textContent = JSON.stringify(data, null, 2);
  } catch (err) {
    resultBox.textContent = "Request failed: " + err.message;
  }
};
</script>

</body>
</html>
